<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="My RC"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyRCapp"
        tools:targetApi="31">
        <activity
            android:name=".Chat.Adapters.UserListActivity"
            android:exported="false" />
        <activity
            android:name=".Chat.ChatListActivity"
            android:exported="false" />
        <activity
            android:name=".Chat.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".TutorsActivity"
            android:exported="false"
            android:label="@string/title_activity_tutors"
            android:theme="@style/Theme.MyRCapp" />
        <activity
            android:name=".Profile_users"
            android:exported="false"
            android:label="@string/title_activity_profile_users"
            android:theme="@style/Theme.MyRCapp" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".RegistrationActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.MyRCapp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>